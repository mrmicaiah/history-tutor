{
  "name": "history-tutor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit -p worker/tsconfig.json && tsc --noEmit -p pages/tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "db:migrate:local": "wrangler d1 migrations apply history_tutor_db --local",
    "db:migrate:remote": "wrangler d1 migrations apply history_tutor_db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^4.87.0"
  }
}
